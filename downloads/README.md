# Client downloads

Player binaries the porch site serves.

| File | Role |
| --- | --- |
| `greenhaven.exe` | Windows client copy |
| `greenhaven-linux` | Linux client copy (stripped release) |

Pages link `/downloads/greenhaven.exe` and `/downloads/greenhaven-linux`. Rebuild then recopy after a distribution build.
